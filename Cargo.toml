[package]
name = "gss"
version = "0.1.0"
edition = "2024"

[dependencies]
lex-just-parse = { version = "1.3.1" }

[features]
default = ["interning"]
interning = ["lex-just-parse/interning"]
internal-api = []
value-enum = []

[[bin]]
name = "gss_test"
path = "src/bin/main.rs"
